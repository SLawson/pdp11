; File: SO_06.mac
; Tests: Add carry
.PSECT A, REL

START:
         MOV      #2, R6
         SEC
         ADC      R6 ; Should result in 3 in R6
         ADC      R6 ; Carry bit should be clear, so no change to R6
         CCC
         HALT

