; File: SO_07.mac
; Tests: Subtract carry


START:
         MOV      #2, R1
         SEC
         SBC      R1 ; Should result in 1 in R1
         SBC      R1 ; Carry bit should be clear, so no change to R1
         CCC
         HALT

