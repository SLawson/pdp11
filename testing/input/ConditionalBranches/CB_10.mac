; File: CB_10.mac
; Tests: Branch if >
.PSECT A, REL

; L2 should not execute
L2:
        MOV   #2, R3
        HALT

; L1 should execute
L1:
        MOV   #1, R3
        HALT

START:
        CCC
        SEC
        BHI   L2
        CLC
        SEZ
        BHI   L2
        SEC
        BHI   L2
        CLC
        CLZ
        BHI   L1
        HALT

